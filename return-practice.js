const formatGreeting = (name) => {
  return `Welcome back , ${name}`;
};

const greeting = formatGreeting("Prashant");
console.log(greeting);

const greetingMessage = formatGreeting("Purushottam");
console.log(greetingMessage);
